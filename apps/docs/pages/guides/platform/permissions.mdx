import Layout from '~/layouts/DefaultGuideLayout'

export const meta = {
  id: 'permissions',
  title: '许可协议',
  description: 'Supabase云主机环境的权限要求',
}

Supabase平台在每个项目附带的Postgres数据库之上提供额外的服务（如存储）。这些服务默认将其操作数据存储在你的数据库中，以确保你对其保持完全的控制。

然而，这些服务假定对他们的数据有一个基本的访问水平，以便例如能够在上面运行迁移。打破这些假设有可能导致这些服务在你的项目中无法运行。

- 存储 "模式下的所有权利都由 "超级基地_存储_管理员 "拥有。
- `auth'模式下的所有权利由`supabase_auth_admin'拥有。

违反这些假设有可能不会导致立即中断，而是在以后有了新的迁移时才生效。

export const Page = ({ children }) => <Layout meta={meta} children={children} />

export default Page
